Run xorg based application on wayland
#### Install
```shell
sudo pacman -S xorg-xwayland
```

determine if app is run with xwayland
```shell
sudo pacman -S xorg-xeyes
```
with command xeye, if app is running with xwayland then the eyes will move